# FAILURES.md — honest, updated per run

- Open-domain transfer: not supported. Reason: command store is site-specific.
- Free-text goals now DO drive the plan, but only within one workflow, and the parser is keyword-based, not an LLM. `planner.plan_goal()` extracts budget / RAM / PIN from the goal and routes to `phone_delivery_check`; `run_variant` logs a `PLAN` event. Precedence is explicit call args > goal-derived params > command defaults. Goals whose domain has no mocks (medicine, travel, groceries) or that match no known workflow return `status: ABSTAIN, failed_constraint: unsupported_goal` instead of silently running the phone workflow. Limits to say on stage: 1 workflow, English keyword matching, and the 3-day delivery window is fixed by the workflow rather than parsed from the goal. Open-domain transfer remains unsupported.
- Learn/version-bump loop: DEMONSTRATED locally 2026-09-12, still NOT in the pitch (owner call). `reflect.propose_bump` mutates the command file, re-runs the full 5-variant guard, ACCEPTs only if every variant still passes, else REJECTs; `rollback` restores from `commands/.history/`. Logged in `logs/versions.jsonl`: 1 ACCEPTED (v1 to v2), 1 REJECTED (bad lesson, budget=100, caught by variant 1+4), 1 ROLLED_BACK. Gate to un-cut "learning" on stage = 3+ logged trials plus a rehearsal.
- Variant 1 end-to-end: VERIFIED 2026-09-12 (Python 3.11.9, Playwright chromium, mocks :8000): `status: pass`, Pixel Lite 8GB Rs 18999, delivery available to 500001, 2 extractive evidence rows, APPROVAL_REQUESTED+GRANTED, hash chain in logs/. See last_run.json + evidence.html.
- Perturbed recovery (modal/extra-step/rename/shuffle/throttle): VERIFIED 2026-09-12. Variant 4 composite (seed 42): pass, 2 extra steps (both overlay dismissals), detect ~4ms, heal ~210-223ms across repeat runs. Rename+strip forces synonym re-ground (0.392 -> 0.80). Interstitials show once per tab session (cookie-modal realism).
- Storefront restyle v2 (VoltKart + SwiftShip) RE-VERIFIED 2026-09-12: all DOM contracts frozen (data-testid, aria role/name, card `data-name`/`data-price`/`data-ram`, exact `Apply Filter` / `Check Delivery` button text needed by chaos.js rename and by `detector.scan`). Full guard green after restyle: v1 pass/0 extra, v2 pass/0 extra, v3 ABSTAIN, v4 pass/2 extra, v6 ABSTAIN. Side effect fixed: the Site A evidence snippet was the card emoji (U+1F4F1); thumbnail is now an inline SVG with no text node, so `snippet_verbatim` is the product name `Pixel Lite 8GB`.
- Real-site credibility run (books.toscrape.com): not done. Reason: blocked behind variant 1.
