# 2026-09-12 — Full project understanding

## What was done
Read and analyzed every file in the SLAB/SENTRY project to build a complete mental model.

## Project identity
- **Name:** SENTRY — a recovery-and-proof layer for browser agents
- **Competition:** SLAB Hackathon, Track 01 (Adaptive Browsing)
- **Thesis:** Replay deterministically. Heal on change. Prove every claim. Abstain when wrong.
- **Not** a shopping agent — the recovery engine is the product; the cross-site workflow is the benchmark/test bench.

## Architecture summary (14 agent .py files, ~1153 lines; harness ~1617 lines)
- **CLI** (`__main__.py`): run / learn / rollback / propose-bump
- **Planner** (`planner.py`): keyword-based NL goal -> workflow + params (budget, RAM, PIN); unsupported domains ABSTAIN
- **Replayer** (`replayer.py`, 312 lines): the core — 4-step workflow (open search -> apply filter -> extract cheapest -> check delivery loop). Detect-recover-verify on every step. Hash chain on stdout. Evidence claims with SHA-256. Constraints check (pass/ABSTAIN).
- **Grounder** (`grounder.py`): 5 fused signals (selector, role+name, text, visual, landmark), weighted scoring, tau=0.70 threshold, synonym tolerance
- **Detector** (`detector.py`): scans for modals, renamed buttons, extra steps, error pages; postcondition verification
- **Recovery** (`recovery.py`): 3-level ladder (re-plan dismiss, re-locate with relaxed signals, backtrack to last-good-URL); never acts irreversibly on unverified state
- **Evidence** (`evidence.py`): extractive-only claims (verbatim snippet + char offset + SHA-256)
- **Constraints** (`constraints.py`): positive proof required ("2-3 days"); rejects negative-tagged strings ("not available")
- **Approval** (`approval.py`): browser modal (primary) + CLI y/N (fallback)
- **Reflect** (`reflect.py`): regression-guarded version bumps; guard runs all 5 variants; ACCEPT/REJECT/ROLLED_BACK logged to versions.jsonl
- **Learner** (`learner.py`): wraps webcmd CLI adapters

## Mock sites (port 8000)
- VoltKart (site_a): search.html + product.html — 3 phone products, filterable by price/RAM
- SwiftShip (site_b): check.html — PIN delivery check (per-product edge case: Nova X1 undeliverable)
- chaos.js: 6 perturbation types (shuffle, rename, strip, modal, extra_step, throttle) + composite; 600ms poll of judge console /status

## Harness (port 8765)
- Flask console + dashboard (768-line SPA): 5 scenario presets, param overrides, live SSE trace, chaos injector, run history
- Agent runs as subprocess (Playwright sync API can't run in Flask thread)
- Regression tests: test_constraints.py (negative-match), test_mock_contracts.py (DOM invariants), test_guard.py (placeholder)
- transfer_probe.py: books.toscrape.com credibility test (v5 PASS)
- demo_video.py: scripted 8-beat demo video recorder

## Verified state (as of 2026-09-12)
- v1: pass, 0 extra steps
- v4 composite: pass, 2 extra steps, detect ~4ms, heal ~210-223ms
- v3 (bad PIN): ABSTAIN
- v6 (infeasible budget+RAM): ABSTAIN naming max_price AND min_ram
- v5 (books.toscrape.com): PASS, ground confidence 1.0 on unseen DOM
- Jitter x5: 5/5 pass, heal 186-200ms
- Learn/reflect loop: 1 ACCEPTED (v1->v2), 1 REJECTED (bad lesson), 2 ROLLED_BACK
- 11/11 pytest green

## Open tasks
- Medicine-finder swap (replace phone v0 with pharmacy/clinic mocks)
- README architecture diagram + metrics from real logs
- Venue checklist (VENUE.md) — fill in first 15 min at competition

---

# 2026-09-12 (later) — Production hardening pass

## What was done
User asked for production-scale improvements + a browser-window fix + a real adaptive-browsing test bench.

## Changes made
1. **Browser window fix** — new `agent/browser.py`. Headed Chromium now opens maximized, is raised to the
   foreground via Win32 `SetForegroundWindow` (matched by "Chrome"/"Chromium" window class), lands on the current
   screen, holds `--keep-open` ms (default 6000) after the run, `--slowmo` default 250. New flags in `__main__.py`:
   `--slowmo`, `--keep-open`, `--no-raise`, `--channel`. Logs `BROWSER_WINDOW` event. This fixed the user's
   complaint that the tab opened behind everything and finished before it could be seen.
2. **Chaos engine expanded** — `mocks/chaos.js` now has rename, strip, move, attrs, modal, extra_step, throttle,
   ab, swap, composite, chaos_max. `harness/perturbations.py` rewritten as the catalogue (single source of truth).
3. **Structural grounding** — `agent/grounder.py` rewritten: 5-signal fusion now falls back to ARIA role +
   accessible name + label association + SYNONYMS; added `discover()` for structure-first lookup.
4. **Adaptive waits** — `agent/detector.py`: added `settle()` (DOM-quiet MutationObserver) and `wait_for_post()`
   (poll to deadline). `agent/recovery.py` rewritten to use structural relocate + generic dialog dismissal.
   `agent/replayer.py` uses `_fill()` by label, adaptive card extraction from the live list, and adaptive waits.
5. **Mock sites hardened** — site_a/site_b carry aria-labels/roles/landmarks; page-side JS reads structurally
   (`maxPriceEl`/`minRamEl`, `pinEl`/`statusEl`/`checkButton`) so a testid strip doesn't break the site's own logic.
6. **Tests** — 62 total green: `test_mock_contracts.py` (id + structure), `test_chaos_engine.py` (catalogue↔chaos.js
   parity, rename↔synonym parity), `test_grounding.py`, `test_constraints.py` (fast unit), and
   `test_adaptive_live.py` (22 live browser tests over every perturbation).
7. **Console** — generic `/perturb/<type>`, `/api/perturbations` catalogue, 12 chaos buttons, new presets.

## Key bug found & fixed
Judge-operated (live, 600ms-poll) injection ABSTAINed because the agent used a fixed 300ms wait and scanned before
chaos landed (~600ms after load). Replaced with `settle()` + `wait_for_post()`. Live composite now passes with
recovery logged. This was a genuine production-grade fix, not cosmetic.

## Verified state after hardening
- Full guard green: v1 pass/0, v2 pass/0, v3 ABSTAIN, v4 pass/2, v6 ABSTAIN
- Deterministic perturbations: rename/strip/move/attrs/modal/extra_step/throttle/swap all PASS
- ab/chaos_max ABSTAIN honestly (only ineligible cards remain) — correct, documented in DECISIONS.md
- 62/62 tests green; last_run.json + evidence.html regenerated (pass, extra 2, heal 242ms, Pixel Lite 8GB)

## Re-verification (same day, fresh session)
Re-ran the whole battery from a clean checkout state to confirm nothing had drifted:
- 40 fast unit tests green (0.06s); 22 live adaptive tests green (130.27s); 62/62 total.
- Headed run `--slowmo 200 --keep-open 3000 --variant 4 --perturb composite` → pass, extra_steps 2,
  avg_time_to_detect 16ms, avg_time_to_heal 375ms, elapsed 8083ms, both evidence rows hashed.
- `BROWSER_WINDOW` log confirms `raised: true`, `screen_w: 1536, screen_h: 960` (current screen detected).
- compute_metrics: replay_success_rate 285/285, citation_precision 1.0, transfer pass (books.toscrape.com),
  recovery_entries 453, avg heal 238.1ms / detect 256.3ms.
- last_run.json + evidence.html rebuilt from the verified run (2 rows).
- Console `/api/perturbations` serves the full 12-type catalogue; `/` returns 200.

## Session 2 — UX + robustness pass (catalogue, window, dashboard)

User's asks: make the frontend clearer about what it does; have the goal drive which workflow
runs; add more products to the site; fix the browser window (multiple tabs/windows, does not
come to the front, stays open after the run).

### Shell blockers (both fixed, not code bugs)
- `python` is NOT on PATH on this machine. Use the absolute path
  `C:\Users\home\AppData\Local\Programs\Python\Python311\python.exe` (has playwright+flask).
- PowerShell rejects bash's `\` line continuation — that was the second error.
- Added `run.cmd` launcher: probes for a Python with playwright+flask, then
  `start` / `dash` / `run <args>` / `test [args]` / `py <script>`.
  GOTCHA: `shift` does NOT update `%*` in cmd.exe — v1 silently forwarded the subcommand to
  pytest (`ERROR: file or directory not found: test`). Fixed by rebuilding the arg tail.

### Code changed
- `mocks/site_a/search.html`: 3 → 24 products rendered client-side from a `CATALOG` array
  (brands, Rs 8,499–49,999, 4–16GB, ratings, 2 out-of-stock). Cheapest 8GB listing
  (Lava Blaze Rs 9,999) is OOS **on purpose** so "cheapest in-stock" is load-bearing.
- `agent/replayer.py`: extraction reads availability (explicit `data-stock`, else the visible
  stock line), excludes OOS cards, logs `STOCK_FILTER`, adds a distinct `no_in_stock_match`
  ABSTAIN. Added `"cards"` to the pass payload so the answer is independently re-derivable.
  Default headed `keep-open` 6000→1500ms.
- `agent/browser.py`: `hold_open()` no longer re-raises on a timer (it fought the operator).
- `mocks/site_b/check.html`: per-product outcomes — unserved at hub / 5–7 days (outside the
  3-day SLA) / standard 2–3 days.
- `harness/console/app.py`: new `POST /api/plan` (dry-runs the real planner), `GET /api/docs`,
  `GET /api/docs/<name>` (allow-listed; traversal → 404), `GET /api/rubric`; headed runs from
  the console default `--keep-open 4000`.
- `harness/console/static/index.html`: **Goal Interpreter** panel (workflow + step chain +
  params + refusals, shown before the run), **Audit & Docs** explorer (7 docs live from disk),
  window-behaviour note, site buttons reuse named tabs instead of piling up windows.
- Tests 93 → **109 green**. New `test_console_api.py`; catalogue-shape + in-stock guards.
  Answer tests now re-derive the expected pick from the returned candidate set rather than
  pinning "Pixel Lite 8GB" (which failed as soon as a cheaper in-stock option existed — the
  test was wrong, not the code).

### Verified
- v1 clean: pass, extra 0, picks Redmi Note 8GB Rs 12,999, skipping the Rs 9,999 OOS trap.
- Full suite: **109 passed in 212s**.
- Dashboard screenshots: interpreter showed "4 steps" + `phone_delivery_check`; docs panel
  listed all 7 files with live mtime.
- `/api/plan`: delivery → 4 steps · enquiry → 5 steps · medicine → refused, `workflow: null`.
- Console running as background task `Wjrn2r` on :8765; mocks on :8000.

### Notes for next time
- Launch background servers with `run_in_background: true`; `nohup ... &` and `(cmd &)` both
  die when the bash session ends.
- `cmd //c` from Git Bash mangles quoting — verify command wrappers via Python `subprocess`.
- Console UI is dark (operator tool) vs light mock storefronts (simulated products) — deliberate.

